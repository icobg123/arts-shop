import 'react';

/*
declare module 'react' {
  /!**
   * ViewTransition component for React 19 experimental builds
   * Enables view transitions for child components during navigation
   *!/
  export const unstable_ViewTransition: React.ComponentType<{
    children: React.ReactNode;
    name?: string;
  }>;

  /!**
   * Alias for unstable_ViewTransition (if importing directly as ViewTransition)
   *!/
  export const ViewTransition: typeof unstable_ViewTransition;
}
*/
